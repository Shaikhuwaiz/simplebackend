services:
  - type: web
    name: simplebackend
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm run start"
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: 7001
